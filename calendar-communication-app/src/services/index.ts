import { fetchCompanies, fetchCommunications, addCompany, updateCompany, deleteCompany } from './api';

export {
  fetchCompanies,
  fetchCommunications,
  addCompany,
  updateCompany,
  deleteCompany,
};